<div class="container-fluid">
    <div class="row">
      <div class="panel">
      <div class="small-4"></div>
      <div class="small-4 small-offset-4 text-center" text-center>
        <!--Sidebar content-->
        Search: <input type="text" placeholder="Type your query here" ng-model="query">
        Sort by:
        <select ng-model="orderProp">
          <option value="title">Alphabetical</option>
          <option value="rank">Highest Rank</option>

        </select>
        <a href="" class="movies button secondary radius tiny" ng-click="reverse=!reverse;">Order</a>

      <div class="small-4"></div>
      </div>
      </div>
    </div>
    <div class="row">
      <div class="small-4"></div>
      <div class="small-4 small-offset-4 text-center">
        <!--Body content--><!--
        <ul class="phones">
          <li ng-repeat="phone in phones | filter:query | orderBy:orderProp">
            <span>{{phone.title}}</span>
            <p>{{phone.rank}}</p>-->
          
          <ul class="movies small-block-grid-1">
            <li ng-repeat="movie in movies | filter:query | orderBy:orderProp:reverse" class="panel" style="padding-top: 20px;">
              <a href="imdb250.html#/movies/{{movie.rank}}" class="thumb"><img ng-src="/data/images/{{movie.imdbID}}.jpg"></a><br>
              <a href="imdb250.html#/movies/{{movie.rank}}"><span class="imdbSearchTitle">{{movie.title}}</span></a>
              <p class="imdbSearchDesc">Rank: {{movie.rank}}</p>

            </li>
          </ul>
          
          </li>
        </ul>

      </div>
      <div class="small-4"></div>

    </div>
</div>

